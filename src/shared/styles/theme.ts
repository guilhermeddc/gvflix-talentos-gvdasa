import {createTheme} from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#E30613',
    },
    secondary: {
      main: '#85888B',
    },
    background: {
      default: '#000000',
      paper: '#1B1B1B',
    },
  },
});
